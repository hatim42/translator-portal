{
  "name": "translator-forms-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:live": "node --test tests/live-site-boundaries.mjs"
  }
}
